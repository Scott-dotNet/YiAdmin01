# 1、 Yi01 -- DB & Common

Model

+ `SystemConfig` - 系统配置

+ `appsettings.json` 中配置数据

  > ```c#
  > // 数据库连接字符串
  > public string DBConnectionString { get; set; }
  > ```

在Common

+ 全局常量 GlobalConstant

+ 全局环境 GlobalContext 

在 DAL

+ 添加包